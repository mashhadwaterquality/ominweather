name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5  # ← آپدیت

      - name: Set up JDK 17
        uses: actions/setup-java@v5  # ← آپدیت
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v4  # ← آپدیت

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Build Debug APK
        run: ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v5  # ← آپدیت
        with:
          name: OminWeather-APK
          path: app/build/outputs/apk/debug/app-debug.apk
